<%= form_for(@issue_report) do |f| %>
  <% if @issue_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue_report.errors.count, "error") %> prohibited this issue_report from being saved:</h2>

      <ul>
      <% @issue_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
